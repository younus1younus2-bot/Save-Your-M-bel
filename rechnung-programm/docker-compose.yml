# Start: docker compose up -d   → http://localhost:3000
services:
  portal:
    build: .
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file: .env
    volumes:
      - ./data:/app/data
